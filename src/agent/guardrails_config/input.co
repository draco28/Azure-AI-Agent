define flow self check input
  $allowed = execute self_check_input
  if not $allowed
    bot refuse to respond
    stop

define bot refuse to respond
  "I'm sorry, I cannot process this request as it appears to violate our usage policy. I can help you with document queries and file status checks."
